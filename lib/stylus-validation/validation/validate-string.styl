//
// VALIDATE STRING
// ===============
//


validate-string($test, $allow-ident = false)

  if typeof($test) == 'string'
    return

  else if typeof($test) == 'ident' && $allow-ident == true
    return

  else
    error('Expected string, got ' + -string($test))

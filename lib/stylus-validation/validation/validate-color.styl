//
// VALIDATE COLOR
// ==============
//


validate-color($test)

  if type($test) == 'rgba' || type($test) == 'hsla'
    return

  else if type($test) == 'ident' && $test == transparent
    return

  else
    error('Expected a color, got a ' + -string($test))
